# go-lang-learning
Repository to learn Go Lang

## Commands to run

- To run a Go program, use the following command:

```bash
go run <filename>.go
```

- To build a Go program into an executable, use:

```bash
go build <filename>.go
```
